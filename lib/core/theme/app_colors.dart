import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Base
  static const background = Color(0xFF0D0D0D);
  static const surface = Color(0xFF1A1A1A);
  static const surfaceLight = Color(0xFF2A2A2A);
  static const accent = Color(0xFF00E676);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFB0B0B0);
  static const textDim = Color(0xFF707070);

  // Phase colors
  static const work = Color(0xFF00E676);
  static const rest = Color(0xFF42A5F5);
  static const preparation = Color(0xFFFF9800);
  static const tabataRest = Color(0xFFAB47BC);
  static const finished = Color(0xFF78909C);
}
